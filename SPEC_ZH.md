# Item Labeller - 技术规格说明书
> **状态**: 草稿
> **版本**: 2.0
> **最后更新**: 2026-04-16
---
## 1. 概述
**项目名称**: Item Labeller（物品标注系统）
**项目类型**: AI驱动的物品识别系统
**核心功能**: 从摄像头、手机相机或文件上传获取图像；使用YOLO进行目标检测；使用Qwen/Gemma VLM进行物品识别；与库存进行匹配并计算置信度分数；显示带有边界框和摘要栏的结果。
**目标用户**: 需要识别和编目物品的库存管理人员、仓库操作员、零售员工。
---
## 2. 系统架构
### 2.1 高层架构
```
┌──────────────────────────────────────────────────────────────────┐
│                         Web 应用程序                              │
│                   (Next.js - 摄像头 + 上传界面)                   │
└──────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────┐
│                      图像处理模块                                  │
│                   (验证、缩放、归一化)                             │
└──────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────┐
│                   YOLO 目标检测 (端口 8001)                        │
│                   检测图像中的所有物体                             │
└──────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────┐
│                   VLM 物品识别 (端口 8004)                        │
│                 生成描述 + 与库存进行匹配                          │
└──────────────────────────────────────────────────────────────────┘
                                  │
        ┌─────────────────────────┴─────────────────────────┐
        │                                                   │
        ▼                                                   ▼
┌───────────────┐                                   ┌───────────────┐
│   已匹配物品   │                                   │   未知物品     │
│ (存在于库存中) │                                   │ (不在库存中)   │
└───────────────┘                                   └───────────────┘
        │                                                   │
        └─────────────────────┬─────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    带边界框的检测结果                              │
│                  每个框显示标签 + 置信度分数                       │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                     标注栏 (摘要)                                 │
│              底部白色条带，显示检测数量和列表                      │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                   Label Studio (QA 仪表板)                       │
│              查看所有检测结果、统计信息和批量审核                   │
└──────────────────────────────────────────────────────────────────┘
```
### 2.2 组件说明
| 组件 | 技术栈 | 用途 | 端口 |
|-----------|------------|---------|------|
| Web 应用 | Next.js + TypeScript | 用户界面（采集和结果展示） | 3000 |
| YOLO 服务 | FastAPI + Ultralytics | 目标检测（所有物体） | 8001 |
| VLM 服务 | vLLM + Qwen/Gemma | 物品识别和匹配 | 8004 |
| 库存 API | FastAPI + SQLite + FAISS | 库存存储和搜索 | 8003 |
| Label Studio | Docker + PostgreSQL | QA 仪表板（仅查看，不做修正） | 8080 |
---
## 3. 功能规格
### 3.1 图像采集
**支持的来源**:
- 摄像头（浏览器 MediaDevices API）
- 手机相机（通过二维码连接 Web 应用）
- 图片上传（拖放或文件选择器）
**图像要求**:
- 格式：JPEG、PNG、WebP
- 最大分辨率：4096x4096
- 最小分辨率：100x100
**用户流程**:
1. 用户选择采集来源（摄像头/手机/上传）
2. 用户拍摄/选择图像
3. 图像进行预处理（缩放、归一化）
4. 图像发送至处理流程
### 3.2 目标检测 (YOLO)
**模型**: YOLOv8（基于 COCO 预训练，检测通用物体）
**任务类型**: 带边界框的目标检测
**置信度阈值**: 0.25（可配置）
**关键行为**:
- 检测图像中的所有物体（不仅限于库存物品）
- 未知物品会被标记但不会被忽略
- 检测在库存匹配之前进行
**输出格式**:
```json
[
  {
    "class": "cell phone",
    "confidence": 0.95,
    "bbox": [x, y, width, height]
  },
  {
    "class": "book",
    "confidence": 0.87,
    "bbox": [x, y, width, height]
  }
]
```
### 3.3 物品识别 (VLM)
**模型**:
- 主模型：Qwen2-VL（7B 或根据 GPU 内存选择更小版本）
- 辅助模型：Gemma（用于交叉验证，可选）
**识别流程**:
1. 对每个检测到的物体，使用 Qwen2-VL 生成描述
2. 将描述转换为嵌入向量
3. 在 FAISS 索引中搜索 top-k 库存匹配项
4. 如果匹配分数 ≥ 阈值 → 标记为 **已匹配** ✅
5. 如果无匹配或分数较低 → 标记为 **未知** ❌
6. 返回带置信度分数的结果
**输出格式**:
```json
{
  "bbox": [x, y, width, height],
  "detected_class": "cell phone",
  "detected_confidence": 0.95,
  "vlm_description": "A black smartphone with a rectangular screen",
  "match_status": "matched",
  "matched_item": {
    "id": 5,
    "name": "iPhone 14",
    "confidence": 0.92
  },
  "confidence_score": 0.92
}
```
### 3.4 匹配逻辑
**匹配状态**:
| 状态 | 条件 | 显示 |
|--------|-----------|---------|
| ✅ 已匹配 | 最佳匹配分数 ≥ 0.70 | 绿色边框，物品名称 |
| ❌ 未知 | 最佳匹配分数 < 0.70 | 红色边框，VLM 描述 |
**置信度显示**:
- 每个边界框显示：`[物品名称] 92%`
- 未知物品显示：`[?] 未知 78%`
### 3.5 标注栏（摘要）
**位置**: 图像底部
**高度**: 在图像高度基础上增加 50px
**背景**: 白色 (#FFFFFF)
**内容**:
```
┌────────────────────────────────────────────────────────────────┐
│ [带边界框的图像]                                               │
│                                                                │
│                                                                │
├────────────────────────────────────────────────────────────────┤
│ 摘要：已匹配：3 件物品 | 未知：2 件物品                          │
│ • iPhone 14 (1)  • 控制面板 (2)  • [?] 未知 (2)                │
└────────────────────────────────────────────────────────────────┘
```
**摘要格式**:
```
摘要：已匹配：{n} 件物品 | 未知：{n} 件物品
• {物品名称} ({数量})  • {物品名称} ({数量})  • [?] 未知 ({数量})
```
### 3.6 Label Studio (QA 仪表板)
**用途**: 不用于修正 - 仅用于查看和统计
**使用场景**:
1. **检测 QA**: 查看多张图像的所有检测结果
2. **统计**: 了解哪些物品经常被检测到/未知
3. **批量审核**: 上传 50 张图像，一次查看所有检测结果
4. **未来训练**: 收集图像后，用于后续标注
**配置**:
- 模板：图像 + RectangleLabels（只读显示模式）
- ML 后端：已连接但设置为只读显示模式
- 导出：JSON/CSV 格式的检测报告
---
## 4. 用户界面
### 4.1 Web 应用程序页面
| 页面 | 路由 | 用途 |
|------|-------|---------|
| 首页 | `/` | 带上传选项的仪表板 |
| 采集 | `/capture` | 摄像头/手机采集界面 |
| 上传 | `/upload` | 拖放式文件上传 |
| 结果 | `/results/:id` | 显示带框和摘要的检测结果 |
### 4.2 结果展示
**带边界框的图像**:
```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│     ┌─────────────────┐                                 │
│     │ 控制面板         │  ← 绿色边框，"控制面板 95%"      │
│     │            95%  │                                 │
│     └─────────────────┘                                 │
│                        ┌────────┐                        │
│                        │手机    │  ← 绿色边框，"iPhone 92%"│
│                        │    92% │                        │
│                        └────────┘                        │
│  ┌──────────┐                                           │
│  │书籍      │  ← 红色边框，"[?] 未知 78%"               │
│  │     78%  │                                           │
│  └──────────┘                                           │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 摘要：已匹配：2 件物品 | 未知：1 件物品                    │
│ • 控制面板 (1)  • iPhone 14 (1)  • [?] 未知 (1)         │
└──────────────────────────────────────────────────────────┘
```
### 4.3 组件规格
**边界框覆盖层**:
- 已匹配物品：绿色边框 (#22C55E)，白色标签背景
- 未知物品：红色边框 (#EF4444)，白色标签背景
- 标签位置：边界框左上角
- 标签格式：`{名称} {置信度}%`
**标注栏**:
- 高度：50px
- 背景：白色
- 文字：黑色，14px 字体
- 格式："摘要：已匹配：N | 未知：N"
- 物品列表：水平排列，圆点分隔
**相机采集组件**:
- 相机预览（实时视频元素）
- 拍摄按钮（大号，居中）
- 相机切换器（如果有多个相机）
- 权限处理（拒绝时显示友好错误）
**图片上传组件**:
- 拖放区域
- 文件选择按钮
- 选择后预览
- 上传进度指示器
---
## 5. 技术规格
### 5.1 服务器要求
| 要求 | 规格 |
|-------------|---------------|
| 操作系统 | Linux（推荐 Ubuntu 22.04） |
| GPU | NVIDIA GPU，16GB+ VRAM |
| CUDA | 12.0+ |
| Docker | 24.0+ |
| Docker Compose | 2.0+ |
### 5.2 服务端口
| 服务 | 端口 | 协议 |
|---------|------|----------|
| Web 应用 | 3000 | HTTP |
| YOLO 服务 | 8001 | HTTP |
| VLM 服务 | 8004 | HTTP（OpenAI 兼容） |
| 库存 API | 8003 | HTTP |
| Label Studio | 8080 | HTTP |
### 5.3 环境变量
```bash
# 服务器
NVIDIA_VISIBLE_DEVICES=0
CUDA_VISIBLE_DEVICES=0
# YOLO 服务
YOLO_MODEL=yolov8n.pt
DETECTION_THRESHOLD=0.25
# VLM 服务
VLM_MODEL=Qwen/Qwen2-VL-7B-Instruct
VLM_MAX_TOKENS=256
# 匹配
MATCH_THRESHOLD=0.70
# Label Studio
LABEL_STUDIO_TOKEN=your_token_here
LABEL_STUDIO_URL=http://label-studio:8080
# 库存
INVENTORY_DB=/data/inventory.db
INVENTORY_INDEX=/data/inventory.index
```
### 5.4 API 端点
**YOLO 服务** (`http://localhost:8001`):
```
POST /detect
  Body: multipart/form-data { image: file }
  Response: [{class, confidence, bbox}]
GET /health
  Response: {status: "ok"}
```
**VLM 服务** (`http://localhost:8004`):
```
POST /v1/chat/completions
  Body: {model, messages}
  Response: {choices: [{message: {content}}]}
GET /health
  Response: {status: "ok"}
```
**库存 API** (`http://localhost:8003`):
```
GET /items
  Query: ?page=1&limit=20
  Response: {items: [...], total: 100}
POST /items
  Body: {name, description, category}
  Response: {id, name, ...}
GET /items/{id}
  Response: {id, name, ...}
PUT /items/{id}
  Body: {name, description, category}
  Response: {id, name, ...}
DELETE /items/{id}
  Response: {success: true}
POST /items/search
  Body: {embedding: [...]}
  Response: {matches: [{item_id, name, score}]}
POST /items/import
  Body: multipart/form-data {file: csv}
  Response: {imported: 50}
GET /items/export
  Query: ?format=json|csv
  Response: file download
```
**处理 API** (`http://localhost:8002`):
```
POST /process
  Body: multipart/form-data { image: file }
  Response: {
    detections: [
      {
        bbox: [x, y, w, h],
        detected_class: string,
        detected_confidence: float,
        vlm_description: string,
        match_status: "matched" | "unknown",
        matched_item: {id, name, confidence} | null,
        confidence_score: float
      }
    ],
    summary: {
      total: int,
      matched: int,
      unknown: int,
      items: [{name, count}]
    },
    image_with_boxes: base64
  }
```
---
## 6. 数据流程
### 6.1 完整检测流程
```
┌─────────────────────────────────────────────────────────────────┐
│                      图像上传/采集                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      图像预处理                                   │
│  - 验证格式（JPEG、PNG、WebP）                                   │
│  - 如果 > 4096x4096 则缩放                                       │
│  - 生成缩略图                                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      YOLO 目标检测                               │
│  - 运行 YOLOv8 推理（预训练模型，未微调）                        │
│  - 检测所有物体                                                  │
│  - 按置信度阈值过滤                                              │
│  - 提取边界框                                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VLM 物品识别                                  │
│  对每个检测到的物体：                                            │
│  1. 裁剪 ROI（感兴趣区域）                                      │
│  2. 发送到 Qwen2-VL 获取描述                                     │
│  3. 转换为嵌入向量                                               │
│  4. 在 FAISS 中搜索匹配项                                        │
│  5. 分类为"已匹配"或"未知"                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      结果组装                                    │
│  - 构建带所有元数据的检测数组                                    │
│  - 统计已匹配与未知数量                                          │
│  - 生成物品摘要列表                                              │
│  - 渲染带边界框的图像                                            │
│  - 在底部添加标注栏                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      最终输出                                    │
│  - 带框+标签+置信度的图像                                         │
│  - 摘要栏（白色，高度 50px）                                     │
│  - JSON 响应包含所有检测数据                                     │
└─────────────────────────────────────────────────────────────────┘
```
---
## 7. 错误处理
### 7.1 错误类型
| 错误 | 处理方式 | 用户提示 |
|-------|----------|--------------|
| 相机访问被拒绝 | 显示备用界面 | "相机访问被拒绝。请在浏览器设置中启用。" |
| 图像格式无效 | 拒绝并提示消息 | "不支持的格式。请使用 JPEG、PNG 或 WebP。" |
| 图像过大 | 自动缩放或拒绝 | "图像过大。最大尺寸为 4096x4096。" |
| YOLO 超时 | 重试一次后失败 | "检测失败。请重试。" |
| VLM 不可用 | 仅使用 YOLO 类别 | "AI 描述不可用。显示基础检测结果。" |
| 库存为空 | 全部显示为未知 | "库存为空。请添加物品以启用匹配。" |
| 网络错误 | 指数退避重试 | "连接丢失。正在重试..." |
### 7.2 优雅降级
- 如果 VLM 失败：仅使用 YOLO 类别标签（不进行库存匹配）
- 如果 FAISS 失败：显示检测结果但不进行匹配（全部标记为未知）
- 如果 Label Studio 不可用：系统仍可正常运行，仅跳过 QA 仪表板
---
## 8. 未来增强（v1 不包含）
| 增强功能 | 状态 |
|-------------|--------|
| 基于库存物品对 YOLO 进行预训练 | 未来 |
| 人工修正工作流 | 未来 |
| 从修正中主动学习 | 未来 |
| 基于修正数据微调模型 | 未来 |
| 实时视频检测 | 未来 |
| 条码/二维码集成 | 未来 |
---
## 9. 术语表
| 术语 | 定义 |
|------|------------|
| ROI | 感兴趣区域 - 包含检测到物体的裁剪区域 |
| VLM | 视觉语言模型 - 同时处理图像和文本的 AI 模型 |
| FAISS | Facebook AI 相似度搜索 - 用于快速相似度搜索的向量存储 |
| 已匹配 | YOLO 检测到的、与库存中某物品匹配的物体 |
| 未知 | YOLO 检测到的、与库存中任何物品都不匹配的物体 |
| 标注栏 | 添加在图像底部的白色条带，显示检测摘要 |
| 置信度分数 | 检测/匹配正确的概率（0-100%） |
---
## 附录 A: 文件结构
```
baoma-item-labeller/
├── docker-compose.yml
├── Dockerfile.base
├── .env.example
├── README.md
├── SPEC.md
├── docs/
│   └── deployment.md
├── services/
│   ├── yolo/
│   │   ├── Dockerfile
│   │   ├── app.py
│   │   └── requirements.txt
│   ├── vlm/
│   │   ├── Dockerfile
│   │   └── config.py
│   ├── inventory-api/
│   │   ├── Dockerfile
│   │   ├── app.py
│   │   └── requirements.txt
│   ├── process/
│   │   ├── Dockerfile
│   │   ├── app.py
│   │   └── requirements.txt
│   └── preprocess/
│       ├── Dockerfile
│       ├── app.py
│       └── requirements.txt
├── web-app/
│   ├── package.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   ├── tsconfig.json
│   └── src/
│       ├── app/
│       ├── components/
│       │   ├── Camera/
│       │   ├── Upload/
│       │   └── Results/
│       ├── lib/
│       └── styles/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
└── data/
    ├── inventory.db
    └── inventory.index
```
---
## 附录 B: 输出示例
### B.1 检测结果 JSON
```json
{
  "image_id": "img_123",
  "detections": [
    {
      "bbox": [100, 50, 150, 200],
      "detected_class": "cell phone",
      "detected_confidence": 0.95,
      "vlm_description": "A black smartphone with a rectangular screen",
      "match_status": "matched",
      "matched_item": {
        "id": 5,
        "name": "iPhone 14"
      },
      "confidence_score": 0.92
    },
    {
      "bbox": [300, 100, 200, 150],
      "detected_class": "remote control",
      "detected_confidence": 0.88,
      "vlm_description": "A small rectangular device with buttons",
      "match_status": "unknown",
      "matched_item": null,
      "confidence_score": 0.45
    }
  ],
  "summary": {
    "total": 2,
    "matched": 1,
    "unknown": 1,
    "items": [
      {"name": "iPhone 14", "count": 1},
      {"name": "[?] 未知", "count": 1}
    ]
  }
}
```
### B.2 摘要栏文本
```
摘要：已匹配：1 件物品 | 未知：1 件物品
• iPhone 14 (1)  • [?] 未知 (1)
```
---
*技术规格说明书 v2.0 结束*